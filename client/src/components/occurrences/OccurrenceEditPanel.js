import React from "react";
import { useSpring, animated } from "react-spring";
import { useMeasure } from "../../hooks/useMeasure";

const OccurrenceEditPanel = ({ showPanel }) => {
  // const [bind, { height }] = useMeasure();
  // const props = useSpring({ height: showPanel ? height : 0 });
  // return (
  //   <div { ...bind } >
  //     <animated.div>
  //     </animated.div>
  //   </div>
  // );
  return <></>;
};

export default OccurrenceEditPanel;
